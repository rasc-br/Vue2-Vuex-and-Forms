export interface Profile {
    firstName: string,
    lastName: string,
    company: string,
    favoriteBand: string,
    mood: string
}
export interface PopupStatus {
    one: boolean,
    two: boolean,
    three: boolean
}
